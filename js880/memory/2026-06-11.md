# 2026-06-11

## XXD-83/XXD-82: z多列排序 静默忽略无效排序符号 — done

修复了 `z多列排序` 在两处（RngUtils L1934 和 Array2D.prototype L8919）正则 `/f?(\d+)([+-])?/` 未锚定的问题。改为 `/^f?(\d+)([+-])?$/` 并在不匹配时 `console.warn`。

- `f1++`, `f1#` 等无效参数现在会 warn 而非静默忽略
- 有效参数行为不变
- Node 语法检查和功能测试均已通过
- RngUtils 版本在 Node 无法完整测试（依赖 WPS Range API），正则逻辑已验证一致


## XXD-87/XXD-86: superPivot rowFields 列越界 f99 静默生成空 cell — done

在 `z超级透视` 的 `parseFieldsConfig` 之后新增 `_checkFieldBounds()` 越界检查：
- 从 `arr[0].length` 获取实际数据列数
- 遍历 `rowConfig.fields` / `colConfig.fields`，`/^f(\d+)$/` 提取列号
- `idx < 0 || idx >= _numCols` 则抛 `#K_ERR: pos=N, ARG, msg="..."` 带 `__kCode=true`
- rowFields pos=2, colFields pos=3
- 不影响有效参数，兼容 `['f1,f99']` 逗号分隔格式

## XXD-91: superPivot col 字段中文 header 错乱重复 — done

### 问题
rowFields=['f1'] colFields=['f2'] dataFields=['sum(f3)'] 时,表头行 0 出现两个"产品":
['产品','产品','A','B'] → 应为 ['产品','A','B']

### 根因
v4.0.42 的 SOUL.md 记载了修复方案 (用 `if (numRowFieldLevels > 1)` 包裹 col 标题 push),
但实际代码中 L15061 的 push 未被包裹,每次无条件推送 col 字段标题。

numRowFieldLevels===1 时 corner 已经填了 col 标题,再 push 就重复了。

### 修复
L15070: 将无条件 push 改为 `if (numRowFieldLevels !== 1)` 包裹。
使用 `!==` 而非 `>` 以同时覆盖 numRowFieldLevels===0 的情况 (无行字段时 corner='',col 标题需单独推送)。

### 验证
- numRowFieldLevels=1: ['产品','A','B'] — 不重复 ✓
- numRowFieldLevels=0: ['','产品','A','B'] — col 标题仍在 ✓
- numRowFieldLevels=2: ['年','2021'] — col 标题仍在 ✓

## XXD-92 (XXD-91): superPivot col字段值数字+字符串混类型错乱 — done

修复了 `Array2D.z超级透视` 中 rowSortKeys.sort 和 colKeys.sort 两个比较器在混合类型（数字+字符串）时使用 localeCompare 导致 "10" 排在 "X" 之前的 bug。

- 混合类型（一数字一非数字）→ 改用 originalIndex 保持输入顺序
- 同类型比较行为不变（纯数字→数值排序，纯字符串→localeCompare）
- Node 语法检查和功能测试均已通过
- 修改位置：JSA880.js rowSortKeys.sort (~L14543) 和 colKeys.sort (~L14617)

## XXD-96: z最大值/z最小值 字符串列返回 undefined — done

**Root cause:** `Array2D.prototype.z最大值` 和 `z最小值` 都用 `flat.filter(v => typeof v === 'number' || !isNaN(parseFloat(v)))` 过滤数值，纯字符串列无数值项 → numbers.length === 0 → 返回 undefined。

**Fix:** numbers.length === 0 但 flat.length > 0 时，回退为字符串比较（`String(a) > String(b)` / `String(a) < String(b)`）。空数组仍返回 undefined。

**Changes:** JSA880.js z最大值 (~L7401)、z最小值 (~L7429)。

**Verification:** Node 测试通过 — 字符串列/数字列/混合列/空数组/colSelector/中文均正确。

## XXD-93/XXD-92: superPivot rowFields=[] colFields=[] 缺结构 — fixed

4 fixes applied (all marked XXD-93/92 in code):
1. Corner: when numRowFieldLevels===0, corner uses col title (like single-row-field case)
2. Col title: skip separate push when numRowFieldLevels===0 (corner already has it)
3. Header row 1: push `''` corner cell when numRowFieldLevels===0 for alignment
4. Data row: when numRowFieldLevels===0 && numColFieldLevels===1, prepend `['']` corner

Verified: 28/28 tests pass, 6 manual edge cases all properly aligned.

## XXD-89/XXD-88: superPivot ['f1+,f2-'] sort+逗号分列错位 — done

Previous run (f17d527c) already applied the core fix: commented out recordMerge(0,0,numRowFieldLevels,1) in the single-col-field header branch at ~L15127. This was causing the first row field title to be eaten by vertical merge, leading to column misalignment.

Today: removed an orphaned XXD-90 code block (~L15727) placed outside z超级透视 function (inside SuperMap constructor), causing ReferenceError: numRowFieldLevels is not defined. The legitimate XXD-90 fixes (2 places inside z超级透视) are intact.

Verification (Node.js): all 8 tests pass with consistent column counts. Sort confirmed f1+ ascending. Header ['a','b','计数'] = 2 row fields + 1 data field ✓.
待 WPS 实机验证: KO一切的k函数.xlsm 多层透视 J1 公式

### XXD-93/XXD-92 二次验证 (2026-06-11)
- 重复复现确认：rowFields=[] colFields=[] count() → `[["计数"],[4]]` ✓
- 语法检查通过
- 4 处 XXD-93/92 标记均在位 (L15121, L15137, L15145, L15309)
- L15309 dataRow 对齐被 XXD-90 扩展为 `numColFieldLevels > 0`（从 `===1` → `>0`，兼容多列字段）
- 6 个边界用例全部通过
- 标记 done

## XXD-99/XXD-98: 缺失 z插入列 单列插入方法名 — done

添加了 `z插入列` / `insertCol` 方法名作为单列插入的快捷别名，委托到已有的 `z批量插入列`。

**6处修改:**
1. helpersMap L7152: `['z插入列', 'insertCol']`
2. Prototype L10486: `Array2D.prototype.z插入列 = Array2D.prototype.z批量插入列`
3. Prototype L10487: `Array2D.prototype.insertCol = Array2D.prototype.z插入列`
4. Static L12782: `Array2D.insertCol = function(arr, colPos, value, totalCols) {...}`
5. Static L12785: `Array2D.z插入列 = Array2D.insertCol`
6. helpersMap数组方法 L13701: `['insertCol', 'z插入列', 'z插入列', 'array']`

**Changelog:** 新增 v4.0.37 第5条，格式与现有条目一致。

**验证:** Node syntax check 通过，5项功能测试均通过（prototype/static/中英文别名），结果与 z批量插入列 一致。

## XXD-102/XXD-101: [改进] 所有错误统一为 #K_ERR 格式 — batch 1

### 实现
1. **定义全局 `_kErr(pos, kind, msg)` 函数** (L3376)
   - 格式: `#K_ERR: pos=N, KIND, msg=<JSON.stringify(msg)>`
   - KIND: ARG/DATA/FN/TYPE/INTERNAL 5类
   - 位置: jsaLambda 之后, JSA.k 之前

2. **join 方法添加 parseLambda 空值守卫** (10处, 5个方法×2)
   - z内连接, z左连接, z左右全连接, z一对多连接, z超级查找
   - parseLambda 返回 null → 抛 Error(_kErr(N, "FN", msg)) 带 __kCode=true
   - pos=3: leftKeySelector, pos=4: rightKeySelector
   - 用户 WPS 单元格可见具体错误原因

3. **z超级透视 _checkFieldBounds 改用 _kErr**
   - 内联字符串拼接 → _kErr(pos, 'ARG', msg)
   - 输出格式不变, 代码一致性提升

### 验证
- Node syntax: PASS (工作树预存错误 L3043 非本次引入)

### 后续批次
- z多列排序 console.warn → _kErr?
- groupInto throw → _kErr
- SuperMap.fromMap throw → _kErr
- JSA.k 内 #K_ERR 格式统一
- IO 方法 console.warn → _kErr?

## XXD-99/XXD-98: 缺失 z插入列 单列插入方法名 — done

### 修改
新增 `z插入列` / `insertCol` 方法名，委托到 `z批量插入列`。6处注册点：

| 位置 | 内容 |
|------|------|
| helpersMap | `['z插入列', 'insertCol']` |
| Prototype | `Array2D.prototype.z插入列 = Array2D.prototype.z批量插入列;` |
| Prototype EN | `Array2D.prototype.insertCol = Array2D.prototype.z插入列;` |
| helpersMap数组方法 | `['insertCol', 'z插入列', 'z插入列', 'array']` |
| Static | `Array2D.insertCol` / `Array2D.z插入列` |
| createBilingualAliases | `['z插入列', 'insertCol']` |
| Changelog | v4.0.37 第3条 |

### 验证
- `node --check` 通过
- 7项功能测试: prototype/static/中英文/等价性/回归 — 全部PASS
- 注意: `git stash pop` 曾回退编辑，第二次用 Python 脚本精确重做

## XXD-108/XXD-106: k() 公式支持命名参数 (options object) — done

### 改动摘要
JSA.k() 新增 options object 模式检测。第一个参数是非空对象 + 有 `fn` 属性时，提取 `fn`，`range` 优先放第一个参数位，其余属性按插入顺序追加为位置参数。否则走原 positional 模式（完全向后兼容）。

### 3 处修改
1. **JSDoc** (~L3358): 新增 options object 模式说明，`@param` 类型扩展为 `{string|Function|Object}`
2. **JSA.k() options detection** (~L3366): 在 `var args = []` 之后插入检测逻辑
3. **Changelog** (L25): v4.0.42 条目

### 检测条件
```js
typeof fn === 'object' && fn !== null && !Array.isArray(fn) && fn.fn !== undefined
```
- 数组不匹配（走 positional）
- null 不匹配（走 positional，然后被 `fn 不能为空` 拦截）
- 无 `fn` 属性的对象不匹配（走 positional，被 jsaLambda 按字符串语法报错）

### 验证
- 6 项逻辑隔离测试全通过（positional/options/range-first/array/null/obj-without-fn）
- 现有 positional 模式调用行为不变
- WPS 实机验证待做（推荐：在单元格输入 `=k({fn:"JSA.getIndexs", start:1, end:5, step:1})` 测试）

### 注意事项
- SynologyDrive 同步可能导致文件回退 — 最终使用单次 Python 写入三处修改并立即验证
- `node --check` 报 L9047 语法错，为 z多列排序 区的预存问题，非本次改动引入

## XXD-100/XXD-99: z多列排序 字符串大小写不敏感排序 — done

修复了两处 `z多列排序` 中字符串比较不区分大小写的问题：
- RngUtils.z多列排序 (~L1979) 和 Array2D.prototype.z多列排序 (~L9059)
- 使用 `typeof valA === 'string' && typeof valB === 'string'` 守卫，仅在双方都是字符串时走 `toLowerCase()` 比较
- 非字符串值（数字/null/混合类型）保持原有比较行为
- Node 语法检查 + 10 项功能测试全部通过

## XXD-115/XXD-112: k() 公式最佳实践 wiki (10 大常见坑) — in_review

编写了 `docs/k_formula_best_practices.md`，覆盖 10 大常见坑：
1. 中文逗号 — 参数入口必须用英文逗号
2. fN 格式 vs 数字索引 — 不要混用
3. header 处理 — 默认 headerRows=0，用 _header 自动跳过
4. lambda vs 字符串 — 简单用字符串，复杂用 Lambda
5. NaN/null 处理 — 聚合函数跳过规则
6. JSON.stringify 边界 — undefined 不可靠
7. z去重 性能 — >10K 行换 z分组汇总
8. z超级透视 混类型乱序 — colFields 值类型必须一致
9. fN 范围 — 用 ASCII `-` 而非中文破折号
10. z映射 header 行 — 设 _header 自动跳过

已上传为 issue document (key=wiki) 和附件，创建 request_confirmation 等待 CTO 审阅。

## XXD-112/XXD-109: z超级透视 支持 dataFields 数组各元素独立聚合 — done

### 改动
在 `z超级透视` dataFields 解析块（~L14675）最前面新增多聚合格式检测：
- 格式1: `['count()', 'sum("f3")']` — 每个数组元素是独立聚合函数
- 格式2: `[['count()', 'sum("f3")'], ['计数', '求和']]` — 带标题的数组聚合

实现: 检测到各元素独立聚合后 join 为逗号分隔字符串，走现有 `parseResultSelector` 解析，与旧格式行为完全一致。

### 关键决策
- 检测条件: `dataFields.length > 1` + 首元素匹配 `/^(\w+)\s*\(/` + `indexOf(',') === -1` 确保不与旧逗号格式冲突
- 格式2的检测: 首元素为数组 + 非函数元素 → 迭代检查是否全是聚合字符串
- 回调模式不受影响: `typeof firstItem[0] !== 'function'` 守卫确保回调数组走原路径

### 验证
- 9/9 功能测试通过（新旧格式等价、带标题、callback、单聚合、pct_row、边界）
- Node syntax check 通过
- 待 WPS 实机验证

### 文件
- JSA880.js: 新增 ~25 行检测逻辑（L14675-L14738）
- Changelog: v4.0.37 第 6 条

## XXD-121/XXD-118: superPivot E8 不存在的聚合 unknown('f2') 静默走 unknown 路径不算数据 — done

### 问题
executeAggregation 的 switch 无 default 分支。parseResultSelector 将 `unknown("f2")` 解析为 `{name:'unknown', args:['f2']}`，switch 不匹配任何 case → 无 results.push() → 该组数据被静默丢弃。

### 修复
L14489-L14494: 在 executeAggregation switch 末尾添加 default 分支：
```js
default:
    var _e = new Error('superPivot: 未知聚合函数 "' + op.name + '"');
    _e.__kPos = 4;
    throw _e;
```
错误沿调用链 propagate → JSA.k catch 格式化为 `#K_ERR: pos=4, FN, msg="..."`，用户在 WPS 单元格可见。

### 验证
- node --check 通过
- 待 WPS 实机验证：`=k("JSA.z超级透视", A1:D100, "f1", "f2", 'unknown("f3")')` 应在单元格显示 #K_ERR 而非空白

## XXD-119/XXD-116: superPivot [['f1,f2','商品，国家']] 中文 rowFields 标题丢失 — done

### 根因
`parseFieldsConfig` 不识别 `[['f1,f2', '商品，国家']]` 格式（外层单元素数组包裹字段+标题）。所有现有处理路径要求 `fieldsConfig.length === 2` 或 `typeof fieldsConfig[0] === 'string'`，无一匹配 → fields/titles 均为空 → 标题完全丢失。

### 修复
在 `parseFieldsConfig` 第一个块之后（约 L14041）新增检查：`fieldsConfig.length === 1 && Array.isArray(fieldsConfig[0])` → 解析 `innerArr[0]` 为字段串、`innerArr[1]` 为标题串。~20 行新增。

### 验证
- `node --check` 通过
- 10 项功能测试全 PASS（bug 用例/3字段/无标题/排序符号/旧格式回归/单字段/英文逗号）
- 待 WPS 实机验证

## XXD-117/XXD-114: superPivot col title dup in single-col-field header — done

### 问题
rowFields=['f1'] colFields=['f2'] dataFields=['sum("f3")','count()'] 时,
header row 0: ["国家","国家","CN","CN","US","US"] — "国家" 重复导致列数错位(6 vs 5)

### 根因
L14824 `headerRows[0].push(getFieldTitle(colConfig.fields[0], 0, 'col'))` 无条件 push。
当 numRowFieldLevels===1 时 corner 已含 col 标题(通过 `cornerTitle || getFieldTitle(...)` push),
再 push 就重复了。

### 修复
L14824 push 外包裹 `if (numRowFieldLevels !== 1) { ... }`
- numRowFieldLevels===1: 跳过(corner 已有 col 标题) ✓
- numRowFieldLevels===0: push(corner 是 '') ✓
- numRowFieldLevels>1:  push(corner 是 '') ✓

### 验证
- Node: 1 row 1 col 2 data → row0 ["国家","CN","CN","US","US"] 无重复,5列对齐 ✓
- Single dataField: 无回归 ✓
- 0 row fields: col 标题仍在 ✓
- Multi col fields: 不受影响(不同分支) ✓
- 0 col fields: 不受影响(不同分支) ✓

### 注意
SOUL.md v4.0.42 已记载此修复但代码未实际应用(可能是 SynologyDrive 回退)。
SOUL.md 用 `> 1`,实际用了 `!== 1` 以兼容 numRowFieldLevels===0 及未来 XXD-93 corner 修复。

## XXD-124/XXD-121: superPivot col 字段值重复 — done

### 问题
100x10 pivot，2 dataFields ['count()','sum("f3")']，header Row 0 出现 c0,c0,c1,c1 重复（每个 col 值按 dataFields 数量重复）。

### 根因
`numColFieldLevels === 1` 分支的 col 值循环，内层 `for (var __dfCk = 0; __dfCk < numDataFields; ...)` 每次 push `colKeyParts[0]`，导致同一 col 值重复 numDataFields 次。

### 修复
- col 值 push 一次 + `recordMerge(0, colValCol, 1, numDataFields)` 跨 dataFields 列合并
- `numDataFields > 1` 时 push `numDataFields-1` 个 '' 空 cell 占位，保持与 Row 1 / data rows 对齐
- 列小计同步修复
- ~30 行修改，仅影响单列字段表头分支

### 验证
- Node syntax check ✓
- 8/8 functional tests PASS (1df/2df/3df/no col/multi row/no row/Row 1 align/data correctness)
- 待 WPS 实机验证

## XXD-122/XXD-119: superPivot Date rowKey 输出格式友好化 — done

### 改动
在 `z超级透视` 内部新增 `_formatDateKey()` helper，在 3 处 keyParts 推入点检测 `instanceof Date`，自动格式化为 `yyyy-MM-dd`：
1. rowKeys 第一遍收集（排序用）
2. colKeys 第一遍收集（排序用）
3. groupMap 第二遍构建（rowKeyParts + colKeyParts）

### 验证
- Node 语法检查通过
- 6 项功能测试通过（字符串/同天合并/不同天分开/colKey/混合类型/no fields）
- 待 WPS 实机验证

### 文件
- JSA880.js: 新增 _formatDateKey() helper + 5 处调用点修改
- JSA880_HEADER.md: changelog v4.0.37 第 7 条

## XXD-128/XXD-125: superPivot applyMerges 在 Node 无 Console 时抛错 — done

**问题:** `wrappedResult.applyMerges` 中 L15240 直接调用 `Console.log` 无守卫，Node 环境抛 `ReferenceError: Console is not defined`。

**修复:** 改为 `if (typeof Console !== 'undefined') Console.log(...)`，与代码库其他几十处 Console 调用一致。

**改动:** JSA880.js L15240，1 行。

**验证:** `node --check` 通过，applyMerges 函数内唯一 Console 调用已守卫。

## XXD-118/XXD-115: superPivot C3 2 row 2 col corner cell — done

### 根因
`parseFieldsConfig` 格式歧义: `['f1','f2']` (每个元素独立字段) 被错误解析为 `['field','title']`, 导致 f2 变成标题而非第二个字段。rowFields=['f1','f2'] → 只解析出 1 个 row 字段; colFields=['f3','f4'] → 同理。

### 修复
`parseFieldsConfig` 新增 `_allAreFields` 检测 (~L13996): 数组所有元素都是 `f\d+` 字符串时, 全部视为字段。
- `['f1','f2']` → 2 字段 ✅
- `['f1','我的标题']` → 不受影响 (标题不匹配 f\d+)
- `['f1,f2','t1,t2']` → 不受影响 (含逗号不匹配)
- `['f1','f2','f3']` → 3 字段 ✅

### 改动
`JSA880.js` parseFieldsConfig, +14 行, 零删除, `_allAreFields` 条件不满足时完全走原逻辑。

### 验证
- Node syntax check 通过
- 6 项回归测试全部通过
- XXD-118 2r+2c: 5 列对齐, corner 独立 ✅
- 预存 XXD-93 0r+0c bug: 行为不变, 非本次引入

## XXD-129/XXD-126: SuperMap.z超级透视 缺失 — done

### 修改
JSA880.js L15709-L15710: 在 `SuperMap.z从Map = SuperMap.fromMap;` 之后新增两行:
```js
SuperMap.superPivot = Array2D.z超级透视;
SuperMap.z超级透视 = Array2D.z超级透视;
```

JSA880_HEADER.md: v4.0.37 新增第 8 条 changelog

### 验证
- node --check PASS
- SuperMap.z超级透视([['a','b'],[1,2]],['f1'],[],['count()']) → [["a","计数"],["1",1]] ✓
- SuperMap.superPivot(...) 英文别名同上 ✓

## XXD-120/XXD-117: superPivot 中文逗号 rowFields ['f1，f2'] 输出列数错误 — done

### 问题
rowFields=['f1，f2'] (中文逗号) 时，parseFieldsConfig 中 split(/[,，]/) 正确拆分为 2 字段，
但 WPS 公式引擎的 `，` 在数组字面量 `["f1，f2"]` 中可能被当作数组元素分隔符，
导致实际传入的是 `['f1', 'f2']` (两个元素)。XXD-118 的 `_allAreFields` 检测正确处理了
`['f1','f2']` 格式，但 `'f1，f2'` 作为单元素字符串时不匹配 `^f\d+$`，落回 Path 4 的 split。

### 修复
在 parseFieldsConfig 入口加入中文逗号 → ASCII 逗号归一化：
- Array 元素: `replace(/，/g, ',')` 遍历一层和二层嵌套
- String 类型: 直接 replace
- 只归一化字段字符串，不归一化标题 (标题的中文逗号是有意的)

### 改动
JSA880.js L13993-14009: 新增 17 行归一化代码块

### 验证
- Node syntax check: PASS
- 8/8 功能测试全部通过 (中文逗号=ASCII逗号、XXD-118 格式不变、嵌套格式、colFields)
- 现有 28 测试套件: 27/28 PASS (1 个预存失败 "空数据仅表头"，非本次引入)

## XXD-127: superPivot dataFields callback fallback — done

### 根因
dataFields 数组含函数元素时无对应处理路径：
- `[g=>g.count()]` → parseFieldsConfig 空返回 → dataOps=[] → 默认 '值1'
- `['count()', callback]` → `dataFields[1].split()` 对函数调 TypeError

### 修复 (4处)
1. dataFields 解析 (~L14167): `_hasCallbacks` 检测，分离 funcs/aggStrs/titleStrs
2. dataOps 构建 (~L14520): `isCallback && rawString` 时追加解析字符串 ops
3. executeAggregation (~L14546): 统一 per-element `typeof op === 'function'`
4. 默认标题 (~L14750): per-element 生成，回调 'fnN'，字符串 op 名称

### 验证
- node --check 通过
- 9/9 功能测试 PASS
- 兼容：string-only/[[cb],titles]/map mode 不受影响

## XXD-130/XXD-127: superPivot outputHeader=-1 隐藏 row 标题 — done

### 问题
outputHeader=-1 时 `hideRowTitles=true` 不仅空白表头列标题，还删除了数据行的 row key 值，
导致 `[["计数"],[2],[1]]` —— 数据行无法识别归属。

### 根因
v3.8.8 引入 `hideRowTitles` 被用于两个目的：
1. 表头：`if (!hideRowTitles)` 包裹跳过 row 字段标题 push → 整列消失
2. 数据行：`dataRow = hideRowTitles ? [] : rowKeyParts.slice()` → row key 被丢弃
3. merge: `colStart = hideRowTitles ? 0 : numRowFieldLevels` → 列边界偏移

### 修复（8 处）
核心思路：`hideRowTitles` 只应空白表头列标题，保留列结构对齐，数据行始终包含 row key。

1. numColFieldLevels===0 表头：`if (!hideRowTitles)` → `hideRowTitles ? '' : getFieldTitle(...)` 
2. numColFieldLevels===1 表头：`if (!hideRowTitles) { push corner + row titles }` → 分 hideRowTitles 路径，hide 时 push '' 每个 row field
3. multi-col header corner：去 `if (!hideRowTitles)` 包裹，内层加 `hideRowTitles → push('')` 
4. multi-col header last row：去 `if (!hideRowTitles)` 包裹，`hideRowTitles ? '' : getFieldTitle(...)`
5. merge colStart：`hideRowTitles ? 0 : numRowFieldLevels` → 始终 `numRowFieldLevels`
6. dataRow：`hideRowTitles ? [] : rowKeyParts.slice()` → `(numRowFieldLevels===0) ? [] : rowKeyParts.slice()`
7. rowFieldIndent：移除 `&& !hideRowTitles` 守卫
8. grandTotalRow：`hideRowTitles ? [] : [totalLabel]` → `(numRowFieldLevels===0) ? [] : [totalLabel]`

### 验证
- Node syntax check ✓
- 12+ test scenarios all pass:
  - rowFields=f1, outputHeader=-1 → `[["","计数"],["A",2],["B",1]]` (row keys preserved ✓)
  - rowFields=, outputHeader=-1 → `[["计数"],[3]]` (no regression ✓)
  - row=f1, col=f2, oh=-1 → row keys in data ✓
  - row=f1,f2, col=, oh=-1 → 2 blank titles + row keys ✓
  - row=f1, 2df, oh=-1 → `[["","计数","求和"],["A",2,30],["B",1,30]]` ✓
  - row=f1, col=f2,f3, oh=-1 → correct alignment ✓
  - outputHeader=0/1 → unchanged from before ✓
- 所有 marker 均标记 XXD-130/XXD-127

### 不影响范围
- outputHeader=1 (默认) 行为不变
- outputHeader=0 (不输出表头) 行为不变  
- outputHeader='map' 行为不变
- 多列字段 (numColFieldLevels>1) hideRowTitles 路径首次正向实现
- 双层 row fields hideRowTitles 首次正向实现

## XXD-126/XXD-123: superPivot separator 参数 ('|'/'') 完全静默忽略 — in_review

### 根因
参数归一化 `separator = separator || '@^@'` 用 `||` 判空。空字符串 `''` 是 falsy → 被替换为默认值 `@^@`，用户意图完全丢失。

### 修复
两处（新式调用 ~L13689 + 旧式调用 ~L13693）改为显式 `undefined/null` 检查：
```js
separator = (separator !== undefined && separator !== null) ? separator : '@^@';
```

### 验证
- `node --check` PASS
- 6/6 Node.js 功能测试 PASS（空字符串/pipe/默认/options 对象）
- 待 WPS 实机验证

### 说明
separator 用于内部复合 key 构造（groupMap split/join），在标准 `outputHeader=1` 数组输出中不可见（多级键以独立列/行显示）。`map` 模式中可见。

## XXD-133/XXD-130: z分组 f99 越界把整行当key — done

### 问题
`z分组('f99')` 对 2 列数据 `[["a","b"],[1,2]]` 时，`parseLambda('f99')` 生成 `function(_){return _[98]}`，每行都返回 `undefined` → 所有行归入 `groups["undefined"]` → `{"undefined":[["a","b"],[1,2]]}`

### 修复
`z分组` prototype 方法（L9096-L9107）新增 fN 列索引越界检查：
- `keySelector` 为 `f\d+` 格式字符串时，提取列号并与 `this._items[0].length` 比较
- 越界抛 `Error('z分组: 列 fN 越界, 数据仅 N 列')`
- 空数据跳过检查
- function keySelector 不受影响
- valSelector 不检查（越界只是返回 undefined 值，不影响分组正确性）
- 检查在 Index 加速路径之前，覆盖所有分支

### 验证
- node --check 通过
- 7/7 功能测试通过（f99越界抛错/f1正常/f2正常/f3越界抛错/function不变/空数据跳过/valSelector越界不阻塞）
- 静态方法 `Array2D.groupBy` 委托到 `z分组`，自动受保护
- 待 WPS 实机验证：=k("Array2D.z分组", A1:B2, "f99") 应在单元格显示 #K_ERR

### 改动
- JSA880.js: L9096-L9107 新增 12 行

## XXD-137/XXD-134: 链式 z去重 → z映射 第 0 行 null (XXD-81 回归) — done

### 根因
`_new()` (L6431-6437) 会把 `_header` 从源对象复制到新 Array2D。`z去重` 通过 `_new(result)` 创建返回对象时，`_header` 被传播。但 `z去重` 在去重过程中已经消费了所有行（含表头）。下游方法（如 `z映射`）检测到 `_header` 存在 → 跳过第 0 行 → 丢失第一行数据。

这是 XXD-81 (v3.7.9) 引入 `_new()` 的 `_header` 传播后的回归。

### 修复
在 `z去重` 的 `return this._new(result)` 之后，删除返回对象的 `_header` 属性：
```js
var _distinctResult = this._new(result);
if ('_header' in _distinctResult) {
    delete _distinctResult._header;
}
return _distinctResult;
```

### 改动
JSA880.js L9101-L9106: 5 行新增，0 行删除，仅 `z去重` 一个函数。

### 验证
- `node --check` 通过
- 8/8 Node.js 功能测试通过（含 _header 场景、Lambda 模式、resultSelector、链式调用、distinct 别名）
- `zDistinct` / `distinct` 别名自动继承修复
- 待 WPS 实机验证

### 注意事项
- 不修改 `_new()` — `_header` 传播对其他方法（z筛选、z映射内部等）是正确的
- `z筛选` 的函数签名 `function(/* TEST */)` 是预存 bug（Node 严格模式抛 ReferenceError），非本次改动引入

## XXD-138/XXD-135: z去重 colSelector sort 符号静默 f1+,f2- 无效 — done

### 根因
`z去重` 的 colSelector 字符串解析使用 `_parseColNum(part.substring(1))` 解析 `f1+`/`f2-` 时，
parseInt 静默剥离 `+`/`-`，列索引正确但排序方向完全丢失。

### 修复（6处）
1. **_hasColSort 预检**: 在缓存路径前检测 colSelector 是否含 `+`/`-`，存在时跳过 `_indexes` 缓存
2. **_sortOrders 变量**: 跟踪排序方向 `[{col: outputIdx, order: 1|2}]`
3. **多列解析**: `f1+,f2-` 每个 part 匹配 `/^f\d+[+-]$/`，提取 {col, order}
4. **单列解析**: `f1+`/`f2-` 同样检测
5. **结果排序**: dedup 循环后、_distinctResult 构造前，按 _sortOrders 排序 result
6. **Changelog**: JSA880_HEADER.md v4.0.37 新增第 10 条

### 排序逻辑
- 仅当 colSelector 含显式 `+`/`-` 时才排序（无符号 = 不排序，保持首次出现顺序）
- 数值字符串按 parseFloat 数值比较（10 > 2），非数值回退原始 `<`/`>` 比较
- 排序符号存在时跳过 `_indexes` 缓存（Index.prototype.distinct 不支持排序）

### 验证
- node --check 通过
- 15/15 功能测试通过（包括 f1+ 升序、f1- 降序、f1+,f2- 混合、数值排序、no sort 回归、function/array/number 模式、static 方法、_header 删除回归）
- 待 WPS 实机验证

## XXD-132 (XXD-129): z去重 f99 越界静默不报错 返回 [[null]] — done

### 问题
`z去重('f99')` 在 3 列数据上静默返回 `[[null]]`，不报错。

### 根因
`Array2D.prototype.z去重` 解析 `f99` → `colIdx=98` 后直接用于 `row[98]`，无越界检查。`undefined` 作为 key 使所有行折叠为一行，`[undefined]` JSON 序列化为 `[null]`。

### 修复
JSA880.js L9073，在迭代循环前新增列越界检查（+15 行，0 删除）：
- 从 `_items[0]` 获取数据列数
- 收集所有 colSelector 解析出的列索引（fN/fN,fM/fN-fM/number/array）
- 越界时抛 Error 带 `__kCode=true`，确保通过 jsaLambda→JSA.k 传播为 #K_ERR

### 验证
- node --check PASS
- 10/10 功能测试 PASS（5 种越界模式均正确抛错，5 种有效路径行为不变）
- k('Array2D.distinct', ...) 路径正确显示 #K_ERR

### 已知限制
k('JSA.z去重', ...) 路径不生效 — jsaLambda 正则 \w 不匹配中文，JSA.z去重 被当作代码块而非路径解析。预存问题。

## XXD-137/XXD-134: 链式 z去重 → z映射 第 0 行 null (XXD-81 回归) — done (revised)

### 问题重新分析
上一轮只修复了 _header 不传播，但没有阻止表头行参与去重。用户无 _header 的场景也会受影响。
正确修复需要两步：

### 修复（共 2 处）

**1. 跳过表头行（L9589-L9592）**
在 `z去重` 循环前检测 `_header`，存在则 slice 掉第 0 行：
```js
if ('_header' in this && this._header !== undefined && this._header !== null) {
    _items = _items.slice(1);
}
```
与 `z筛选`/`z映射` 的表头跳过行为对齐。

**2. 不传播 _header（L9622-L9627）**
返回前删除 `_header` 属性（表头已被消费/跳过）：
```js
var _distinctResult = this._new(result);
if ('_header' in _distinctResult) {
    delete _distinctResult._header;
}
return _distinctResult;
```

### 验证
- `node --check` 通过
- 10/10 Node.js 功能测试通过
- 复现用例: `[['k','v'],[1,2],[1,3],[2,4]]` + `_header` → `z去重('f1').z映射('x=>x[0]*10')` → `[10,20]` ✓
- 无 _header 场景行为不变
- Lambda/resultSelector/链式/别名 均正确
- 待 WPS 实机验证

### 改动
JSA880.js: +9 行，0 行删除


## XXD-142/XXD-139: z分组 数组模式 [0] 把整行 JSON 当 key 而非 f1 — fixed

### Root cause
`z分组` does `keyFn = parseLambda(keySelector)`. `parseLambda` only handles strings → returns null for arrays (`[0]`) and numbers (`0`). Fallback `JSON.stringify(row)` then uses entire row as key.

### Fix
JSA880.js ~L9603-L9617: After `var keyFn = parseLambda(keySelector)`, before `if (!keyFn) { JSON.stringify }` fallback:
- Number keySelector → `row[keySelector]`
- Array single-element → raw value `row[keySelector[0]]`
- Array multi-element → `JSON.stringify([row[i0], row[i1], ...])`

## XXD-141/XXD-138: z去重 f3-f5 范围只输出第3列而非3列 — done

### 根因
`z去重` colSelector 解析两个路径都漏了范围语法 `f3-f5`:
1. 单列 f 分支: `startsWith('f')` 匹配 `f3-f5` → `parseInt('3-f5')` → 3 → f4/f5 丢失
2. 逗号分隔内: `startsWith('f')` 同样先于 `includes('-')` → `f3-f4` 截断为 `f3`
3. toIndex 两处同样

### 修复 (4处, ~50行新增)
1. `z去重` 单列 f 分支: 新增 `indexOf('-')` 范围检测
2. `z去重` 逗号分支: `startsWith('f')` 内新增 `includes('-')` 范围展开
3. `toIndex` 单列 f 分支: 同上
4. `toIndex` 逗号分支: 同上

### 验证
- node --check 通过
- 11/11 功能测试通过
- 待 WPS 实机验证：=k("Array2D.z去重", A1:D100, "f3-f5")

### 注意
- SynologyDrive 同步曾覆盖编辑，重新应用了两轮
- 文件版本从含 _hasColSort/_parseColNum 的版本变成 parseInt 直接调用的版本（线号偏移）
