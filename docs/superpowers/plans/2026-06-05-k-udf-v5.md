# KO k() UDF v5 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 `KO一切的k函数.xlsm` 里的 9 个测试公式全部跑通,把 k() 完整实现合并到 `JSA880.js`,ThisWorkbook 只需 3-5 行 wrapper。

**Architecture:** 单源 `JSA880.js`(770KB)持有所有 k() 实现逻辑(`JSA.k` + 4 个预处理 + `$$` 别名),ThisWorkbook 3-5 行 wrapper 只做 UDF 注册。WPS 公式引擎的 UDF 扫描限制要求 `function k(){}` 在 ThisWorkbook,但实现代码全在 JSA880.js。

**Tech Stack:** JSA(JavaScript for Applications,WPS Office 的 VBA 替代)、ES5 兼容语法、Node.js 仿真测试

---

## 文件结构

| 文件 | 状态 | 责任 |
|---|---|---|
| `js880/JSA880.js` | **修改** | 加 `$$` 别名 + `JSA.k` + chainable parser + error 包装,不动既有 Array2D/JSA |
| `KO一切的k函数.xlsm` | **修改** | ThisWorkbook 替换为 3-5 行 wrapper(去掉 BIN 里的旧 k() 模块) |
| `第03章/3-28/_test_k_v5.js` | **新建** | Node 仿真测试 9 公式 + 5 错误注入 |
| `第03章/3-28/KO_k_udf.js` | **重命名 `[DEPRECATED]_KO_k_udf.js`** | 标记废弃 |
| `第03章/3-28/KO一切的k函数_UDF模块.js` | **重命名 `[DEPRECATED]_KO一切的k函数_UDF模块.js`** | 标记废弃 |
| `第03章/3-28/KO一切的k函数.md` | **修改** | 更新部署流程 + changelog |

---

## 现状基线(任务开始前必读)

`JSA880.js` v4.2.2 已有(本次不重写):
- ✅ `JSA.jsaLambda`(line 2801):含 `fixBacktick`(反引号转换)+ `smartUnwrap`(Range→Value2 + 1x1 flatten)
- ✅ `JSA.k = JSA.jsaLambda`(line 2876):当前是纯别名
- ✅ `function k(fn, ...args)`(line 18820):顶层 UDF,直接调 `JSA.jsaLambda`
- ✅ `function jsaLambda(fn, ...args)`(line 18835):顶层 UDF 别名
- ❌ 没有 `$$` 全局别名
- ❌ 没有错误位置化(`#K_ERR: pos=N, KIND`)
- ❌ 没有 chainable 表达式识别(`$$.superPivot(...).filter(...)` 失败)

---

## 任务列表

### Phase 1:测试基础设施(TDD)

#### Task 1:创建 Node 测试 harness 骨架

**Files:**
- Create: `JS880教案/第03章/3-28/_test_k_v5.js`

- [ ] **Step 1:写测试文件骨架**

```javascript
/**
 * KO k() UDF v5 · Node 仿真测试
 * 跑 9 个 Excel 测试公式 + 5 个错误注入,无需 WPS
 */
'use strict';

// === 模拟 WPS JSA 环境 ===
var JSA = {};
var Array2D = {
    version: function() { return '5.0.0-test'; },
    leftjoin: function() { throw new Error('未实现: leftjoin'); },
    superPivot: function() { throw new Error('未实现: superPivot'); },
    distinct: function() { throw new Error('未实现: distinct'); },
    insertCols: function() { throw new Error('未实现: insertCols'); }
};
var $$;  // 全局别名,稍后由 JSA880.js 设置
var Range = function(addr) { return { Address: addr, Value2: [[1,2],[3,4]] }; };
var Console = { log: function() { /* console.log.apply(console, arguments); */ } };
var Application = {};

// 模拟 globalThis
var globalThis = (typeof global !== 'undefined') ? global : this;
globalThis.$$ = $$;
globalThis.JSA = JSA;
globalThis.Array2D = Array2D;
globalThis.Range = Range;

// === 加载要测试的模块 ===
// 稍后 loadFile('JSA880.js') 或 loadFile('KO_k_udf_v5.js')

// === 测试运行器 ===
var tests = [];
var passed = 0;
var failed = 0;

function test(name, fn) {
    tests.push({ name: name, fn: fn });
}

function assertEqual(actual, expected, msg) {
    if (JSON.stringify(actual) === JSON.stringify(expected)) return;
    throw new Error('assertEqual 失败: ' + (msg || '') +
        '\n  期望: ' + JSON.stringify(expected) +
        '\n  实际: ' + JSON.stringify(actual));
}

function runTests() {
    for (var i = 0; i < tests.length; i++) {
        var t = tests[i];
        try {
            t.fn();
            console.log('✅ ' + t.name);
            passed++;
        } catch (e) {
            console.log('❌ ' + t.name);
            console.log('   ' + e.message);
            failed++;
        }
    }
    console.log('\n' + (tests.length === passed ? '✅ 全部通过' : '❌ ' + failed + ' 个失败'));
    process.exit(failed > 0 ? 1 : 0);
}

module.exports = { test: test, runTests: runTests, assertEqual: assertEqual,
                   JSA: JSA, Array2D: Array2D, $$: $$ };
```

- [ ] **Step 2:写 1 个 smoke test 验证 harness**

在文件末尾加:
```javascript
// === Smoke test ===
test('harness 自身能跑', function() {
    assertEqual(1 + 1, 2);
});

if (require.main === module) {
    runTests();
}
```

- [ ] **Step 3:跑测试,确认 PASS**

Run: `cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/JS880教案/第03章/3-28/" && node _test_k_v5.js`
Expected: `✅ harness 自身能跑` + `✅ 全部通过`

- [ ] **Step 4:Commit**

```bash
cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/"
git add "JS880教案/第03章/3-28/_test_k_v5.js"
git commit -m "test: k() v5 测试 harness 骨架"
```

---

#### Task 2:加 9 个 Excel 公式对应的测试用例

**Files:**
- Modify: `JS880教案/第03章/3-28/_test_k_v5.js`(追加 9 个 test())

- [ ] **Step 1:在 smoke test 之前追加 9 个测试 stub**

在 `// === Smoke test ===` 之前追加:

```javascript
// === 9 个 Excel 公式测试(数据用 JSON 模拟 Range) ===
var dataA2B7 = [['A', 1], ['B', 2], ['C', 3], ['A', 4], ['B', 5], ['C', 6]];
var dataA1C17 = [];  // 17 行 3 列,模拟 A1:C17
for (var i = 0; i < 17; i++) {
    dataA1C17.push([i + 1, 'P' + (i % 3), (i + 1) * 10]);
}
var dataA1H40 = [];
for (var j = 0; j < 40; j++) {
    dataA1H40.push([j+1, 'P' + (j%3), 'C' + (j%4), j+1, 1, 2020 + (j%5), (j%12)+1, j+1]);
}

// T1: leftjoin 路径调用
test('T1: k("$$.leftjoin", ...) 路径调用', function() {
    Array2D.leftjoin = function(arr, brr, lk, rk, sel) {
        return [['joined', 'result']];
    };
    var result = JSA.k('$$.leftjoin', [['A'],['B']], dataA2B7, 'f1', 'f1', 'a.f1,b.f2');
    assertEqual(result, [['joined', 'result']]);
});

// T2: 多参 lambda + 嵌套箭头
test('T2: k("(a,v)=>a.filter(x=>x[1]>v)", arr, val)', function() {
    var result = JSA.k('(a,v)=>a.filter(x=>x[1]>v)', dataA2B7, 2);
    // 期望:第 2 列 > 2 的行
    var expected = dataA2B7.filter(function(x) { return x[1] > 2; });
    assertEqual(result, expected);
});

// T3: lambda 返回 1D 数组
test('T3: k("(arr,v)=>arr.map(x=>[x.f2+v])", arr, val)', function() {
    var result = JSA.k('(arr,v)=>arr.map(x=>[x.f2+v])', dataA2B7, 100);
    // 期望:每行第 2 列 + 100,包成 1 元素数组
    var expected = dataA2B7.map(function(x) { return [x[1] + 100]; });
    assertEqual(result, expected);
});

// T4: lambda 调用 $$.distinct
test('T4: k("arr=>$$.distinct(arr,\'f1\')", arr)', function() {
    Array2D.distinct = function(arr, key) {
        return Array2D.distinct || [['A'],['B'],['C']];
    };
    Array2D.distinct = function(arr, key) {
        // 简单实现:按第 0 列去重
        var seen = {}, result = [];
        for (var i = 0; i < arr.length; i++) {
            if (!seen[arr[i][0]]) { seen[arr[i][0]] = 1; result.push([arr[i][0]]); }
        }
        return result;
    };
    var result = JSA.k("arr=>$$.distinct(arr,'f1')", dataA2B7);
    assertEqual(result, [['A'],['B'],['C']]);
});

// T5: lambda 返回 2D 数组
test('T5: k("(arr,v)=>arr.map(x=>[x.f1,x.f2+v])", arr, val)', function() {
    var result = JSA.k('(arr,v)=>arr.map(x=>[x.f1,x.f2+v])', dataA2B7, 10);
    var expected = dataA2B7.map(function(x) { return [x[0], x[1] + 10]; });
    assertEqual(result, expected);
});

// T6: rest + spread + 嵌套调用
test('T6: k("(...args)=>$$.insertCols($$.superPivot(...args),-1,x=>x.sum())", ...)', function() {
    Array2D.superPivot = function() {
        return [['P0', 'sum', 100], ['P1', 'sum', 200]];
    };
    Array2D.insertCols = function(arr, pos, fn) {
        return [['P0', 'sum', 100, 100], ['P1', 'sum', 200, 200]];
    };
    var result = JSA.k('(...args)=>$$.insertCols($$.superPivot(...args),-1,x=>x.sum())',
                       dataA1C17, 'f1', 'f2', "sum('f3')", 1, 0);
    assertEqual(result.length, 2);
    assertEqual(result[0].length, 4);
});

// T7: block body
test('T7: k("(x,y)=>{y=y+1;return x.map(a=>[a[0],a[2]+\'-\'+y])}", arr, val)', function() {
    var result = JSA.k("(x,y)=>{y=y+1;return x.map(a=>[a[0],a[2]+'-'+y])}",
                       dataA1C17, 1);
    // 期望:每行 [id, 数量+'-'+2]
    var expected = dataA1C17.map(function(a) { return [a[0], a[2] + '-2']; });
    assertEqual(result, expected);
});

// T8: 反引号列名
test('T8: k("$$.superPivot", arr, "...", "f6", "sum(`f4*f5`)...")', function() {
    Array2D.superPivot = function(data, rows, cols, vals) {
        return [['header', 'value'], ['P0', 100]];
    };
    var result = JSA.k('$$.superPivot', dataA1H40, 'f3,f2', 'f6',
                       'sum(`f4*f5`),textjoin(`f4+\'-\'+f5`,`+`)');
    assertEqual(result.length, 2);
});

// T9: rest + spread + chainable filter
test('T9: k("(...args)=>$$.superPivot(...args).filter(...)", arr, ..., "", ...)', function() {
    Array2D.superPivot = function() {
        return [['header'], ['Product1'], ['Product2'], ['Product1']];
    };
    var result = JSA.k("(...args)=>$$.superPivot(...args).filter((x,i)=>i==0 || x.f2=='Product1')",
                       dataA1H40, 'f3,f2', '', "count(),sum(`f4`),textjoin(`f4`,`+`)");
    // 期望:header + 2 个 Product1 行
    assertEqual(result.length >= 2, true);
});
```

- [ ] **Step 2:跑测试,确认全部 FAIL(JSA.k 还没实现)**

Run: `cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/JS880教案/第03章/3-28/" && node _test_k_v5.js`
Expected: 9 个新测试全部 ❌(报 "JSA.k is not a function" 之类),1 个 smoke ✅

- [ ] **Step 3:Commit**

```bash
cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/"
git add "JS880教案/第03章/3-28/_test_k_v5.js"
git commit -m "test: 加 9 个 Excel 公式测试 stub"
```

---

#### Task 3:加 5 个错误注入测试

**Files:**
- Modify: `JS880教案/第03章/3-28/_test_k_v5.js`

- [ ] **Step 1:在 9 个测试后追加 5 个错误注入**

```javascript
// === 5 个错误注入测试 ===
test('E1: k() 空调用 → #K_ERR: pos=0, FN', function() {
    var result = JSA.k();
    if (typeof result !== 'string' || result.indexOf('#K_ERR') !== 0) {
        throw new Error('期望错误字符串,实际:' + JSON.stringify(result));
    }
    if (result.indexOf('pos=0') === -1) {
        throw new Error('期望 pos=0,实际:' + result);
    }
    if (result.indexOf('FN') === -1) {
        throw new Error('期望 kind=FN,实际:' + result);
    }
});

test('E2: k("JSA.xxx") 但 JSA880 未加载 → #K_ERR: INTERNAL', function() {
    // 临时把 JSA.jsaLambda 设为 undefined
    var saved = JSA.jsaLambda;
    JSA.jsaLambda = undefined;
    var result = JSA.k('JSA.getIndexs', 1, 10, 2);
    JSA.jsaLambda = saved;
    if (typeof result !== 'string' || result.indexOf('INTERNAL') === -1) {
        throw new Error('期望 INTERNAL 错误,实际:' + result);
    }
});

test('E3: k("$$yyy") $$yyy 不存在 → #K_ERR: FN', function() {
    var result = JSA.k('$$notExist', 1, 2);
    if (typeof result !== 'string' || result.indexOf('FN') === -1) {
        throw new Error('期望 FN 错误,实际:' + result);
    }
});

test('E4: k("x=>x.b", "abc") 类型错 → 包含 TypeError', function() {
    var result = JSA.k('x=>x.b()', 'abc');
    if (typeof result !== 'string' || result.indexOf('TypeError') === -1) {
        throw new Error('期望 TypeError,实际:' + result);
    }
});

test('E5: k("`a${b}c`", 1) 模板字符串保留 ${}', function() {
    // 这是模板字符串场景,期望 #K_ERR(因为 b 未定义),但 ${} 要保留
    // 我们只验证它没被改成双引号
    var errThrown = null;
    try {
        // 模拟:用 new Function 直接编译,看 b 引用是否被保留
        var fn = new Function('return ' + '`a${b}c`');
        fn();
    } catch (e) {
        errThrown = e;
    }
    if (!errThrown || errThrown.message.indexOf('b is not defined') === -1) {
        throw new Error('模板字符串应被原样保留,实际:' + (errThrown && errThrown.message));
    }
});
```

- [ ] **Step 2:跑测试,确认错误注入全部 FAIL**

Run: `cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/JS880教案/第03章/3-28/" && node _test_k_v5.js`
Expected: 5 个 E 测试全部 ❌,9 个 T 测试全部 ❌,1 个 smoke ✅

- [ ] **Step 3:Commit**

```bash
cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/"
git add "JS880教案/第03章/3-28/_test_k_v5.js"
git commit -m "test: 加 5 个错误注入测试 stub"
```

---

### Phase 2:核心 k() 实现(在 JSA880.js)

#### Task 4:在 JSA880.js 顶部加 `$$` 全局别名

**Files:**
- Modify: `js880/JSA880.js`(在第 440 行附近,LAMBDA_PATTERNS 之前)

- [ ] **Step 1:定位插入点**

Run: `grep -n "LAMBDA_PATTERNS = {" "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/js880/JSA880.js"`
Expected: 输出一个行号(约 530-550 范围)

- [ ] **Step 2:在该行之前插入 `$$` 兜底**

在 JSA880.js 里 `LAMBDA_PATTERNS = {` 之前,插入:

```javascript
// ╔════════════════════════════════════════════════════════════════╗
// ║ [v5.0.0] $$ 全局别名 = Array2D                                  ║
// ║  公式 k("$$.superPivot", ...) 必须能直接访问 Array2D 方法          ║
// ╚════════════════════════════════════════════════════════════════╝
(function _kInitDollarDollar() {
    try {
        var __g = (function() { return this; })();
        if (typeof __g.$$ === 'undefined' && typeof Array2D !== 'undefined') {
            __g.$$ = Array2D;
        }
    } catch (e) { /* 静默失败 */ }
})();

```

- [ ] **Step 3:同时在文件末尾(Array2D 定义完后)再次确认**

定位到 JSA880.js 末尾(找 `}` 关闭整个 IIFE 或 console.log 启动行),在最后一行前追加:

```javascript
// [v5.0.0] Array2D 定义完成后再确认 $$ 指向
(function _kFinalizeDollarDollar() {
    try {
        var __g = (function() { return this; })();
        if (typeof __g.Array2D !== 'undefined' &&
            (typeof __g.$$ === 'undefined' || __g.$$ === null)) {
            __g.$$ = __g.Array2D;
        }
    } catch (e) { /* 静默失败 */ }
})();

```

- [ ] **Step 4:语法 sanity check**

Run: `node -c "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/js880/JSA880.js"`
Expected: 无输出(语法正确)或 warning 不影响

- [ ] **Step 5:Commit**

```bash
cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/"
git add js880/JSA880.js
git commit -m "feat(k): 加 \$\$ 全局别名 = Array2D"
```

---

#### Task 5:加 `JSA.k` 包装函数(带错误位置化)

**Files:**
- Modify: `js880/JSA880.js`(在 line 2876 附近,`JSA.k = JSA.jsaLambda` 之前/之后)

- [ ] **Step 1:定位插入点**

Run: `grep -n "JSA.k = JSA.jsaLambda" "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/js880/JSA880.js"`
Expected: 输出 line 2876

- [ ] **Step 2:把 `JSA.k = JSA.jsaLambda` 替换为包装实现**

替换为:

```javascript
/**
 * [v5.0.0] JSA.k — k() 的完整实现(shim 给顶层 function k 调)
 *
 * 比 jsaLambda 多两层:
 *   1. fn / args 预处理(其实 jsaLambda 内部已做大部分,这里加保险)
 *   2. 错误位置化:#K_ERR: pos=N, KIND, msg="..."
 *
 * @param {string|Function} fn - 字符串函数表达式 / 路径 / Lambda
 * @param {...any} args - 后续参数
 * @returns {*} 函数结果;失败返回 "#K_ERR: ..."
 */
JSA.k = function(fn) {
    var args = [];
    for (var i = 1; i < arguments.length; i++) args.push(arguments[i]);

    // 0) 必传检查
    if (typeof fn === 'undefined' || fn === null || fn === '') {
        return '#K_ERR: pos=0, FN, msg="fn 不能为空"';
    }

    // 1) 框架未加载?
    if (typeof JSA.jsaLambda !== 'function') {
        return '#K_ERR: pos=1, INTERNAL, msg="JSA880 框架未加载,请加载 JSA880.js 加载项"';
    }

    // 2) 调 jsaLambda(已含反引号转换 / Range 转 Value2 / 1x1 flatten)
    var result;
    try {
        result = JSA.jsaLambda.apply(null, [fn].concat(args));
    } catch (e) {
        // 错误位置:pos=0 表示 fn / jsaLambda 自身
        var kind = (e && e.message && e.message.indexOf('TypeError') !== -1) ? 'TYPE' : 'FN';
        return '#K_ERR: pos=0, ' + kind + ', msg="' +
               (e && e.message ? e.message.replace(/"/g, "'") : String(e)) + '"';
    }

    // 3) null / undefined 兜底
    if (result === undefined || result === null) {
        return '#K_ERR: pos=0, FN, msg="jsaLambda 返回 null/undefined,可能 fn 语法错或参数不匹配"';
    }

    return result;
};

// 保留兼容:JSA.k 仍可被 jsaLambda 直接调(无包装的版本)
// 如果需要原始行为,可用 JSA.kRaw = JSA.jsaLambda(本计划不导出,留作未来扩展)

/**
 * [v5.0.0] JSA.k.help — 排错指南
 * 在 JSA 编辑器手动调 JSA.k.help() 打印排错清单
 */
JSA.k.help = function() {
    if (typeof Console === 'undefined') return;
    Console.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
    Console.log('  k() 公式常见问题排查');
    Console.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
    Console.log('❌ #K_ERR: pos=0, FN → fn 字符串解析失败,检查语法');
    Console.log('❌ #K_ERR: pos=1, INTERNAL → JSA880 框架未加载');
    Console.log('❌ #K_ERR: pos=0, TYPE → 类型错(传了不该传的对象)');
    Console.log('❌ #NAME? → ThisWorkbook 没粘 3-5 行 wrapper');
    Console.log('✅ 自检:在任意单元格 =k("JSA.getIndexs", 1, 5, 1)');
    Console.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
};
```

- [ ] **Step 3:语法 sanity check**

Run: `node -c "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/js880/JSA880.js"`
Expected: 无错误输出

- [ ] **Step 4:Commit**

```bash
cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/"
git add js880/JSA880.js
git commit -m "feat(k): JSA.k 包装函数,带错误位置化"
```

---

#### Task 6:更新顶层 `function k()` 调 JSA.k(替代直接调 jsaLambda)

**Files:**
- Modify: `js880/JSA880.js`(line 18820-18829)

- [ ] **Step 1:定位**

Run: `grep -n "^function k(fn, ...args)" "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/js880/JSA880.js"`
Expected: 输出 line 18820

- [ ] **Step 2:替换实现**

把:
```javascript
function k(fn, ...args) {
    try {
        return JSA.jsaLambda(fn, ...args);
    } catch (e) {
        return "#K_ERR: " + (e && e.message ? e.message : String(e));
    }
}
```

替换为(用 ES5 兼容,不用 `...args`):
```javascript
/**
 * [v5.0.0] 顶层 k() UDF — 转发到 JSA.k
 * WPS 公式引擎只扫 ThisWorkbook 顶层 function 作为 UDF
 * 实现代码全在 JSA.k(JSA880.js 内),这里只是转发 shim
 */
function k(fn) {
    return JSA.k.apply(null, arguments);
}
```

- [ ] **Step 3:同样更新 jsaLambda UDF**

把 line 18835 的:
```javascript
function jsaLambda(fn, ...args) {
    return k(fn, ...args);
}
```

替换为:
```javascript
function jsaLambda(fn) {
    return JSA.k.apply(null, arguments);
}
```

- [ ] **Step 4:语法 sanity check**

Run: `node -c "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/js880/JSA880.js"`
Expected: 无错误

- [ ] **Step 5:更新 Workbook_Open 日志加上 JSA.k 自检**

找到 `function Workbook_Open()`(line 18847),在 self-test 那块加一行:

```javascript
// 验证 JSA.k(新的带错误位置化包装)
try {
    var ktest = JSA.k("JSA.getIndexs", 1, 3, 1);
    Console.log("   JSA.k 自检:[" + ktest.join(",") + "]");
} catch (e) {
    Console.log("   ⚠️ JSA.k 自检失败:" + e.message);
}
```

- [ ] **Step 6:Commit**

```bash
cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/"
git add js880/JSA880.js
git commit -m "feat(k): 顶层 k()/jsaLambda() UDF 转发到 JSA.k"
```

---

#### Task 7:把 _test_k_v5.js 改成加载 JSA880.js(走真实路径)

**Files:**
- Modify: `JS880教案/第03章/3-28/_test_k_v5.js`

- [ ] **Step 1:把"加载要测试的模块"段替换为真实加载**

把:
```javascript
// === 加载要测试的模块 ===
// 稍后 loadFile('JSA880.js') 或 loadFile('KO_k_udf_v5.js')
```

替换为:
```javascript
// === 加载真实 JSA880.js(在 Node 下 require 它) ===
var path = require('path');
var fs = require('fs');

// 把 JSA880.js 读出来,在我们的模拟环境里 eval
// (不能直接 require,因为它不是 CommonJS 模块)
var jsa880Path = path.resolve(__dirname, '../../../js880/JSA880.js');
var jsa880Code = fs.readFileSync(jsa880Path, 'utf8');

// 模拟 WPS JSA 的全局上下文
var vm = require('vm');
var sandbox = {
    JSA: JSA,
    Array2D: Array2D,
    Range: Range,
    Console: Console,
    Application: Application,
    $$: undefined,
    globalThis: globalThis,
    console: console
};
// 让 $$ 通过 globalThis 引用
sandbox.globalThis.$$ = undefined;

// eval JSA880.js 在沙箱里
vm.createContext(sandbox);
vm.runInContext(jsa880Code, sandbox);

// eval 完后,实际的对象在 sandbox 里
// 把 Array2D / JSA 拉回到 Node 全局
JSA = sandbox.JSA;
Array2D = sandbox.Array2D;
$$ = sandbox.$$ || sandbox.globalThis.$$;
globalThis.$$ = $$;
```

- [ ] **Step 2:跑测试,确认 T1-T5 + T8 PASS,T6/T7/T9 仍 FAIL**

Run: `cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/JS880教案/第03章/3-28/" && node _test_k_v5.js`
Expected:
- ✅ T1 (leftjoin 路径)— jsaLambda 支持
- ✅ T2 (多参 lambda)— jsaLambda 支持
- ✅ T3 (1D 输出)— jsaLambda 支持
- ✅ T4 (lambda 调 $$.distinct)— $$ 已设
- ✅ T5 (2D 输出)— jsaLambda 支持
- ✅ T8 (反引号)— jsaLambda 已有 fixBacktick
- ❌ T6 (rest + spread)— WPS new Function 不一定支持
- ❌ T7 (block body)— WPS new Function 不一定支持
- ❌ T9 (chainable)— 没有 parseChainableExpression
- ❌ E1-E5 (JSA.k 错误位置化待 Task 5 加完后才能 PASS)

- [ ] **Step 3:Commit**

```bash
cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/"
git add "JS880教案/第03章/3-28/_test_k_v5.js"
git commit -m "test: 改为加载真实 JSA880.js"
```

---

### Phase 3:Chainable 表达式支持

#### Task 8:加 `parseChainableExpression` 函数

**Files:**
- Modify: `js880/JSA880.js`(在 JSA.jsaLambda 之前,line 2800 附近)

- [ ] **Step 1:定位插入点**

Run: `grep -n "^JSA.jsaLambda = function" "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/js880/JSA880.js"`
Expected: 输出 line 2801

- [ ] **Step 2:在 JSA.jsaLambda 之前插入 parseChainableExpression**

在 line 2800 之前插入:

```javascript
/**
 * [v5.0.0] 链式表达式解析
 * 把 "$$(...).filter(...).map(...)" 这类链式 lambda 编译成可执行函数
 * 关键:$$ 作为参数显式传入,保证作用域正确
 */
function _kParseChainableExpression(expr) {
    // 检测是否含链式调用(.filter / .map / .slice / .take / .skip / .sort / .reduce)
    var isChainable = /\.\s*(filter|map|slice|take|skip|sort|forEach|reduce|find|some|every)\s*\(/.test(expr);
    if (!isChainable) return null;

    try {
        // 用 Array.prototype[method].call 包装链式调用
        // 把 $$.method(...) 转成 Array.prototype.method.call(...)
        // 但 simpler:直接把 $$ 当作参数传入,lambda 自己处理
        var fnBody = 'return (function() { var __r = (' + expr + ').apply(null, arguments);' +
                     ' return __r; }).apply(null, __args)';
        // 上面太复杂,直接用 new Function 编译,让 $$ 通过 globalThis 访问
        var fn = new Function('__args', 'return (function() {' +
                              '  var $$ = (typeof Array2D !== "undefined") ? Array2D : this.Array2D;' +
                              '  return ' + expr + ';' +
                              '}).apply(null, __args)');
        return fn;
    } catch (e) {
        if (typeof Console !== 'undefined') Console.log('parseChainableExpression 失败:' + e.message);
        return null;
    }
}

```

- [ ] **Step 3:在 JSA.jsaLambda 里集成**

在 `JSA.jsaLambda` 函数体内(line 2801 之后的开头),`var func = JSA.z解析函数表达式(fn);` 这一行**之前**,插入:

```javascript
// [v5.0.0] 链式调用检测
if (typeof fn === 'string' && /\.\s*(filter|map|slice|take|skip|sort|forEach|reduce|find|some|every)\s*\(/.test(fn)) {
    var chainParser = _kParseChainableExpression(fn);
    if (chainParser) {
        try {
            return chainParser(realArgs);
        } catch (e) {
            if (typeof Console !== 'undefined') Console.log('链式执行失败:' + e.message);
            // 继续走原来的解析流程
        }
    }
}

```

- [ ] **Step 4:语法 check**

Run: `node -c "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/js880/JSA880.js"`
Expected: 无错误

- [ ] **Step 5:跑测试,确认 T9 PASS**

Run: `cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/JS880教案/第03章/3-28/" && node _test_k_v5.js`
Expected: T9 ✅(chainable filter),其他不变

- [ ] **Step 6:Commit**

```bash
cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/"
git add js880/JSA880.js
git commit -m "feat(k): 加 parseChainableExpression 支持 \$\$.method().filter()"
```

---

#### Task 9:加错误注入测试的 E1-E5 实现 + 跑全部测试

**Files:**
- Modify: `JS880教案/第03章/3-28/_test_k_v5.js`(可选微调测试断言)

- [ ] **Step 1:跑测试看 E1-E5 状态**

Run: `cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/JS880教案/第03章/3-28/" && node _test_k_v5.js`
Expected: 看哪些 E 测试通过哪些失败,根据结果调整 JSA.k 错误信息格式

- [ ] **Step 2:逐个修 JSA.k 错误信息(在 JSA880.js)**

根据 E 测试输出,定位哪个 E 失败,回到 `JSA.k`(Task 5 加的)调整错误信息字符串以匹配测试期望格式 `#K_ERR: pos=N, KIND, msg="..."`

- [ ] **Step 3:跑测试,期望全部 PASS**

Run: `cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/JS880教案/第03章/3-28/" && node _test_k_v5.js`
Expected: 15 / 15 PASS(9 T + 5 E + 1 smoke)

- [ ] **Step 4:Commit**

```bash
cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/"
git add "JS880教案/第03章/3-28/_test_k_v5.js" js880/JSA880.js
git commit -m "test+feat: 9 公式 + 5 错误注入全部 PASS"
```

---

### Phase 4:ThisWorkbook wrapper + 清理

#### Task 10:把 JSA880.js v5.0 注入到 KO一切的k函数.xlsm(更新 JDEData.bin)

**Files:**
- Modify: `KO一切的k函数.xlsm` 内的 `xl/JDEData.bin`

- [ ] **Step 1:备份当前 xlsm**

Run:
```bash
cp "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/JS880教案/第03章/3-28/KO一切的k函数.xlsm" \
   "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/JS880教案/第03章/3-28/KO一切的k函数.xlsm.v4.2.2.bak"
```

- [ ] **Step 2:用 inject_jsa880.py 把新版 JSA880.js 注入到 xlsm**

```bash
cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/JS880教案/第03章/3-28/"
python3 inject_jsa880_main.py --target "KO一切的k函数.xlsm" \
    --source "../../../wps-cesuan/收益测算表开发V4.8.51-wps版本.xlsm" \
    --inject-source "js880/JSA880.js" 2>&1 | head -50
```

(注:inject_jsa880_main.py 的具体参数看脚本实现,如果不支持 --inject-source 则改用 inject_jsa880_text.py)

- [ ] **Step 3:验证 JDEData.bin 已更新**

Run:
```bash
unzip -p "KO一切的k函数.xlsm" xl/JDEData.bin | grep -c "JSA.k"
```
Expected: 输出 > 0(说明 JSA.k 字符串在 bin 里)

- [ ] **Step 4:把 JDEData.bin 里的旧 k() UDF 模块标记为不活跃**

这一步需要编辑 JDEData.bin 里的 codemodule 列表,把 v4.2.2 的 `KO_k函数` 模块设成 `lock="true"`(禁用)而不是删掉,避免破坏其他 xlsm。

```bash
# 用 inject_jsa880_text.py 工具,具体参数参考工具的 --help
python3 "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/JS880教案/第03章/3-28/inject_jsa880_text.py" \
    "KO一切的k函数.xlsm" --disable-module "KO_k函数"
```

(若工具不支持,改成手动:解压 xlsm,用文本编辑器打开 JDEData.bin,找到 `<codemodule name="KO_k函数">` 那一段,把外层 `<property lock="false">` 改成 `lock="true"`,重打包)

- [ ] **Step 5:替换 ThisWorkbook 代码模块为 3-5 行 wrapper**

用 `inject_jsa880_text.py` 或类似工具,注入以下内容到 ThisWorkbook 模块:

```javascript
function k(fn) { return JSA.k.apply(null, arguments); }
function jsaLambda(fn) { return JSA.k.apply(null, arguments); }
```

> ⚠️ ThisWorkbook 模块的 codemodule 名字是固定的,直接 overwrite 即可。inject_jsa880_text.py 应该会自动处理同名模块的替换。

- [ ] **Step 6:Commit xlsm 改动(若可能)**

如果 xlsm 改动导致 diff 太复杂无法 commit,跳过此步。xlsm 是二进制文件。

---

#### Task 11:在 WPS 里实测(人工验证)

**Files:** (无,纯人工)

- [ ] **Step 1:打开 KO一切的k函数.xlsm**

Run: 打开 WPS,打开 `KO一切的k函数.xlsm`

- [ ] **Step 2:看 Console 日志**

期望看到:
```
✅ k() UDF 已就绪!（JSA880 v5.0.0）
   测试:在任意单元格输入
   =k("JSA.getIndexs", 1, 10, 2)
   看到 1 3 5 7 9 = 成功!
   JSA.k 自检:[1,2,3]
```

- [ ] **Step 3:测 9 个公式 cell**

依次点开 Sheet5!E2 / H2 / N2 / Q2 / T2,Sheet6!F9 / F15,Sheet8!J1,Sheet9!J1。
Expected: 全部不显示 #NAME? / #VALUE!,显示计算结果(数组溢出)。

- [ ] **Step 4:在空白单元测一个简单公式**

输入: `=k("JSA.getIndexs", 1, 5, 1)`
Expected: 1 2 3 4 5(数组溢出)

- [ ] **Step 5:记录结果**

如果 9 个公式都跑通,在本任务打 ✅。如果有失败,记录 cell 地址 + 错误信息,回到对应 Task 修复。

---

#### Task 12:标记旧文件 [DEPRECATED]

**Files:**
- Rename: `第03章/3-28/KO_k_udf.js` → `第03章/3-28/[DEPRECATED]_KO_k_udf.js`
- Rename: `第03章/3-28/KO一切的k函数_UDF模块.js` → `第03章/3-28/[DEPRECATED]_KO一切的k函数_UDF模块.js`

- [ ] **Step 1:重命名两个旧文件**

```bash
cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/JS880教案/第03章/3-28/"
mv KO_k_udf.js "[DEPRECATED]_KO_k_udf.js"
mv KO一切的k函数_UDF模块.js "[DEPRECATED]_KO一切的k函数_UDF模块.js"
```

- [ ] **Step 2:在文件头加废弃说明**

在 `[DEPRECATED]_KO_k_udf.js` 顶部加:
```javascript
/**
 * ⚠️ [DEPRECATED] 此文件已废弃 (2026-06-05)
 * k() 实现已合并到 JSA880.js 的 JSA.k 中
 * 请改用: ThisWorkbook 里 3-5 行 wrapper:
 *   function k(fn) { return JSA.k.apply(null, arguments); }
 *   function jsaLambda(fn) { return JSA.k.apply(null, arguments); }
 */
```

在 `[DEPRECATED]_KO一切的k函数_UDF模块.js` 顶部加类似说明。

- [ ] **Step 3:Commit**

```bash
cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/"
git add "JS880教案/第03章/3-28/[DEPRECATED]_KO_k_udf.js"
git add "JS880教案/第03章/3-28/[DEPRECATED]_KO一切的k函数_UDF模块.js"
git commit -m "chore: 标记旧 k() UDF 文件 [DEPRECATED]"
```

---

#### Task 13:更新 `KO一切的k函数.md` 文档

**Files:**
- Modify: `JS880教案/第03章/3-28/KO一切的k函数.md`

- [ ] **Step 1:在"30 秒上手"段更新部署流程**

找到 `### 🚀 完整操作步骤` 段,把"粘 UDF 模块到 ThisWorkbook"步骤改为:

```markdown
2. **粘 3-5 行 wrapper 到 ThisWorkbook**
   - WPS → 开发工具 → JSA 编辑器
   - 左侧工程树找到 **ThisWorkbook** 模块
   - 粘入以下 3-5 行代码(就这 3-5 行,k() 实现全在 JSA880.js):
     ```javascript
     function k(fn) { return JSA.k.apply(null, arguments); }
     function jsaLambda(fn) { return JSA.k.apply(null, arguments); }
     ```
   - `Ctrl + S` 保存 → 关闭 JSA 编辑器
```

- [ ] **Step 2:更新"配套文件"表格**

把"配套文件"段的 `KO一切的k函数_UDF模块.js` 改为 `[DEPRECATED]_KO一切的k函数_UDF模块.js`,加说明 "已废弃,k() 实现已并入 JSA880.js"。

- [ ] **Step 3:在 changelog 顶部加 v5.0.0 段**

```markdown
- **v5.0.0** (2026-06-05)
  - **k() 实现代码全量合并到 JSA880.js**(JSA.k + 4 个预处理 + 错误位置化)
  - ThisWorkbook 只需 3-5 行 wrapper(`function k() { return JSA.k.apply(null, arguments); }`)
  - 加 `$$` 全局别名 = Array2D
  - 加 `parseChainableExpression` 支持 `$$.superPivot().filter()` 链式
  - 错误信息格式:`#K_ERR: pos=N, KIND, msg="..."`
  - KO_k_udf.js / KO一切的k函数_UDF模块.js 标记 [DEPRECATED]
  - 9 个 Excel 测试公式(Sheet5×5 + Sheet6×2 + Sheet8×1 + Sheet9×1)全部跑通
```

- [ ] **Step 4:Commit**

```bash
cd "/Users/daidai193/Library/CloudStorage/SynologyDrive-code/"
git add "JS880教案/第03章/3-28/KO一切的k函数.md"
git commit -m "docs: k() v5.0.0 部署流程 + changelog"
```

---

## 验收(全 6 条)

1. ✅ `js880/JSA880.js` v5.0 存在,Console 启动有 `JSA.k ready` 自检
2. ✅ `JSA.k` 含 4 个预处理 + 错误位置化,15 个测试 PASS
3. ✅ `_test_k_v5.js` Node 跑 9 公式 + 5 错误注入全 PASS
4. ✅ `KO一切的k函数.xlsm` ThisWorkbook 3-5 行 wrapper,9 公式 cell 不报 #NAME? / #VALUE!
5. ✅ `KO一切的k函数.md` 更新完成,2 旧文件 [DEPRECATED]
6. ✅ JSA880.js v5.0 加载后,`Application.JSA880` / `JSA` / `Array2D` 全部可用,既有功能不退化
